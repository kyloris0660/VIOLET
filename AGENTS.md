# AGENTS.md

## Cursor Cloud specific instructions

This repository (`AnimeLocalBooru`) is currently an empty/brand-new project. As of initial setup, it contains only a `.gitattributes` file with no application code, dependencies, or build configuration.

- **No services to run**: There is no application, backend, or frontend to start.
- **No dependencies to install**: No `package.json`, `requirements.txt`, `pyproject.toml`, or other dependency manifests exist.
- **No tests or linting**: No test framework or linter is configured.
- **No build system**: No `Makefile`, `Dockerfile`, `docker-compose.yml`, or other build configuration exists.

Once the project gains source code and dependencies, this section should be updated with:
- How to install dependencies
- How to run/build/test the application
- Any non-obvious development caveats

# 生产 Pixiv A2 实施方案

## 43号质量收口续做（已授权）

项目所有者要求执行43号任务，继续同一工作树、分支与PR #153。承接f98e0b0交付及44db0da生产行为，保留原USD9.998387消费和完整metadata；累计上限已授权改为USD30，先通过恢复/追加门禁回归再发起新付费。未轮换令牌的本轮授权持续有效，不要求再次确认。

1. **修复续做与发布边界。** 在既有预算账本原子记录10→30追加，保留模型/价格/逻辑问题身份；恢复保存响应未结算的尝试，区分已付费与有效答案并复用有效子项。补齐完整范围/事实/判断/候选/来源revision门禁、raw完整性、异常replay、负limit、前向实际节流及结果契约的逐项重算。局部回归不用真实付费服务。
2. **完整判断与质量闭合。** 用修正后的有效性核对原31295配对及8769缺项，优先复用成功缓存、补有影响的角色漏答；保持原分母和新增/排除账。逐一保存十例从来源信号到实际查询的因果路径，不硬编码名称或降低独立预期。复用全量副本验证原80例与少量未用于修正的同类样本。
3. **生产、契约与人工交付。** 仅在完整输入和副本质量通过后更新原生产，保护非owned支持和人工资产，做受影响恢复、真实Edge/普通launcher与来源性能验证；不重跑完整non-E2E。更新同一报告/PR、19条审查对账、本机操作记录，并交付带实测样本截图的A2-MANUAL-ACCEPTANCE.zh-CN.md及脱敏私有证据ZIP。工程通过后仍等待Lead复审和Owner验收，不merge/main push/reviewer/关闭线程/A3。

旧HTTP间隔证明缺口由负责人具名接受，仅豁免不可重建的历史；前向2秒节流仍需执行和验证。43号初始方案无schema新增计划；下列真实副本阻塞使必要索引补齐进入本轮既有迁移授权，不做全库重新获取、备份覆盖或基础标签重跑。

### 43号副本交接阻塞：补齐既有模型外键索引

2026-09-13，a4eb011完整副本计划通过，但旧owned投影的概念删除触发关联表逐概念扫描，达到120秒语句上限后事务回滚。只读逐项核对原1 active/66572支持及完整范围未变。pg_catalog与不执行查询的EXPLAIN确认，旧手写迁移没有建立模型index=True已声明的单列外键索引；复合索引的非首列不能覆盖该查找。

依据23号必要迁移及43号沿用安全更新授权，实施前有界审查选择只补5个非唯一索引：source_concept_signal_links.concept_id、source_concept_evidence.signal_id、source_concept_signals.resolution_run_id，以及source_concept_fallback_search_index的source_signal_id和neighbor_signal_id（表名均含既有blombooru_前缀）。复用模型原有Index定义及checkfirst，在既有迁移顺序内补齐；不新增字段/表、不变更业务行、外键、所有权guard、超时上限或身份算法，不重构迁移平台。

先以无真实服务调用的旧schema/fresh schema/重复运行回归验证，再在既有恢复副本用准确数据库/system identifier、原备份恢复证明和保护表前后摘要执行。索引DDL保持事务式提交、5秒锁等待和120秒语句上限；失败即回滚，原数据及诊断保留，不通过删除表或索引来重试。完整新候选副本apply/replay/rollback/reapply、质量和恢复通过后，才对原生产做相同的幂等必要索引补齐，先查活动工作及保护摘要，再按新计划更新派生投影。备份只用于已验证隔离恢复，不覆盖后续原库写入。此处没有Owner/Lead已验收声明。

43号第一步进度：已实现原账本原子追加、响应结算恢复、有效子项与遗漏目标续接、metadata缓存验证、实际等待节流、完整T0/语义输入发布门禁及逐项证据重算。开发回归190项与新增证据回归31项通过（两组有交集，不相加）。原账本已追加到累计USD30，原6110调用/USD9.998387保持；本地匹配1666份raw，29份无法重构或无效文件单列，不当作成功复用。生产仍运行旧候选，质量、完整契约及新版部署尚待本轮后续验证。

项目所有者已明确要求全量执行23号任务。PR #151已接受并合并，PR #152按39号负责人裁决接受并合并至`2b742ca3e49d4b7d361300e98e0b2d9c1a0eb63d`，日常入口已对齐此main。本轮从该基线建立一个A2分支和一个正常PR，不合并、不推main、不触发额外reviewer、不进入A3。

## 三个连续步骤

1. **范围、兼容与累计所有权。** 数据库只读快照固定T0和Media/work/page映射；优先可信来源解决先验冲突，保留未解决归宿。盘点真实metadata、规范化输入和持久裁决缓存。修正artist带页面context的适配问题，保留作者稳定ID和角色/作品上下文。为正式固定scope扩展既有产品run投影，明确A1到全量的事务替换、累计更新与owned rollback。
2. **获取与完整裁决。** 复用既有metadata队列、metadata-only gallery-dl执行器及work checkpoint；缓存与保存payload先复用。请求间隔至少2秒、同一work最多3次尝试并跨续跑累计。复用resolver候选、确定性关系与兼容judgments，通过明确生产入口接入付费裁决。所有新调用、失败、重试共享USD 10上限，按实际模型单价、usage和保守预留控制。
3. **全量生产落地与验收。** 针对实际写入取得备份和独立恢复证据，在副本验证A1过渡、重复执行、输入变化、独立支持、rollback和分批等价，再通过既有profile/controller部署候选并落地有效全量。验证普通搜索、AND/负向/建议/来源chip、真实图片详情和正常launcher；记录全量性能、预算与全部未完成归宿。

## 实现选择和迁移边界

复用`SourceMetadataRecord`、既有队列和`SourceConceptProductRun`/binding/事务模型。先确认现有JSON范围和projection数据能否支持原子累计替换；只有必要时增加字段，迁移方案在执行前写入本方案并先在恢复副本验证。不得放大旧1%–5% canary百分比或通过轮换active run伪造全量。跨批次共享逻辑输入空间，不能建立永久分裂的概念孤岛。

缓存按角色、context、模型、提示词和策略身份判断兼容；不把历史通用ProviderCache计数当作裁决缓存。原始事实不重写，作者context修复在生产适配层表达并保存版本。

生产裁决先处理作品异名，再将同一resolver实际允许合并的作品组件用于角色上下文，最后裁决角色候选。作品共现标签不作为彼此的所属作品；显式作品/括号上下文和cannot-link仍生效。推导上下文保持原有证据强度，作品身份不会自动授予角色身份。

模型给出的work_context必须能在同一实际作品中找到明确的work角色标签，不能仅因“制服”“兔女郎”等字符串出现在来源中就把它当作所属作品。无法证明的模型上下文保留原值与拒绝原因，不进入硬上下文；来源括号和已接受词汇的上下文证据继续保留。完整原始角色响应仍可复用，不重新调用模型。

实际响应若将输入中确有的标签使用非标准`work_context`角色，适配为`unknown_name_like`且`needs_review`，保留原角色和状态；上下文名称本身不证明角色、地点或作品身份。只恢复同一响应中原有的有效兄弟候选，原始缓存不改写，其他非法角色和输入外名称仍拒绝。

真实响应曾只复述局部确定性提示而遗漏其他名称。角色补全只针对冻结上下文中仍为unknown/person的名称，保留原问题、响应和成功缓存；补全请求明确列出剩余原始标签，以完整真实标签组作为上下文，仍经F7a验证。每个聚合最多完成一次该补全，未知项保留，不循环改写问题追求肯定答案。正常上下文批量可在1–10组内调节，6000输出token和USD10共享上限不变。

尚未处理的聚合直接使用该明确列出目标名称的上下文请求，避免先运行已发现会遗漏名称的旧问题。已完成的旧上下文按原身份读取，明确名称直接保留，补全只覆盖剩余unknown/person。小批预检的成功缓存用于同一冻结输入的后续全量续跑。

剩余名称补全可使用最多两个文本worker，每个独立provider实例持有自己的usage，跨worker共用同一持久预算和认证/限流/连续传输错误暂停门禁。成功单元只有一个writer，先恢复原始付费响应再分配工作；不改变模型、提示词、token上限或Pixiv获取并发。实际启用前先做隔离预算竞争、部分失败和缓存续跑验证。

逐标签核验发现，部分合法F7a记录没有覆盖明确请求的全部写法。补充完成度记账按原问题身份重建目标集合，区分已回答、明确未知、明确非名称和缺失结果；一条记录存在不再代表所有目标已回答。只对原问题中没有任何逐项结果的标签进行一次有界覆盖补偿，已回答的角色、别名、来源标题、未知和非名称不重问。补偿沿用F7a解析器、原始真实上下文、原模型、持久缓存和同一USD10账本，要求每个目标保留候选或明确unknown/non-name处置；有效兄弟候选不改写，再次缺失或预算不足明确记账，不循环索取肯定答案。该修正不把不同图片间的名称或角色互相传播。

配对裁决的作品与其他名称两步共享同一调用暂停状态；401/403/429立即停止后续新请求，连续三次传输失败也暂停。暂停后仍读取成功缓存，未发出的请求不预留或计费。普通解析失败保留原始响应并按既定单项尝试上限处理，不扩大到另一个provider。

本次未增加数据库schema。正式范围、水位、固定Media/work/page和投影版本存入既有run JSON。生产支持使用固定`production_pixiv`键空间，所有批次和代际共用；语义仍由既有resolver决定，支持归属不会通过全局upsert转移其他消费者的行。旧投影通过合法owned rollback退出，新投影在同一事务提交后可见；后续出现未知独立引用时，原guard仍拒绝破坏性撤回。

完整角色事实触发了按字典序根节点合并产生的长链，旧递归find超过Python递归深度。并查集查找改为迭代路径压缩，保持最小字典序根节点和身份决策，不提高全局递归上限；以4096节点降序链、独立分组和不同合并顺序回归验证。失败发生在新配对请求和产品写入之前，固定事实与成功缓存继续复用。

只有固定映射中通过实际来源provenance验证的完整记录进入概念输入。缺失的远端页码单独记账，不影响同一作品已验证的页面；T0后新增作为尾部，不改变既定范围。全部Media、work和page归宿由同一快照命令产生。

## 验证与产物

- focused回归和隔离PostgreSQL覆盖实际修改；一次完整non-E2E，后续仅补受影响项。
- 注册一个A2结果契约，核对固定范围、生产物化、预算、版本、恢复和质量；自动验证不产生负责人接受。
- 独立已知答案验证多语言与困难负例、分批等价，真实全量查询workload记录p50/p95/最大值。
- 一个正常PR、一份中文结果报告、一份本机操作与恢复记录。私有payload、原图、路径和凭据不进入PR。

## 运行保护

保持原生产可用，长时间获取不冻结图库。只在必要切换窗口协调本应用写入。原数据库、存储、认证和模型保留；不重跑导入/分类/WD/本地化，不修改人工标签、相册或确认Entity。A3前置债按39号裁决保留，本轮不运行advanced_full_rescan或改变导入恢复处置。

### 43号质量阻塞时的原候选运行恢复

2026-09-13 副本质量75/80，禁止将新投影更新到原生产。实际普通启动预检发现，生产profile固定44db0da，而开发目录已推进8f10418，canonical_repo_root门禁拒绝下次启动。为保留日常可用性，在独立固定运行目录检出同一已部署44db0da的detached版本，不在该目录开发；A2开发分支、当前副本、旧日志和原生产数据完整保留。这是旧候选的运行恢复，不是第二套实现或新阶段。

执行前备份原profile和正常EXE的launcher-runtime锚点；新目录只创建等价profile，除repo_root外保持原候选、venv、数据库、生产存储、read ON/apply OFF和自动任务设置。先验证git完整身份、Python身份、profile及只读DB预检；端口仍由旧进程占用这一事实单列。确认无活动任务后，仅停止该启动器管理的旧进程，切换锚点，并用普通无参数EXE启动相同44db0da候选。核对实际进程树、自动preflight、完整路径、保护摘要和真实浏览器样本。若启动失败，保留新旧状态和失败证据，使用原备份锚点及明确候选恢复；不覆盖数据库或删除任何原文件。此操作沿用43号已经授权的本应用必要短时停启与恢复范围。


### 43号角色账目闭合检查点

43号角色目标账目闭合：原49227次出现中26343候选、22642非名称、209明确unknown、33次有据尝试耗尽、0未记账。原3组11目标从原raw经通用复合标签来源修正本地恢复，零新增调用。累计6196调用/USD10.497482，未知usage48（含本轮主动暂停的2项），无在途；完整配对、独立质量、新版生产及人工指南仍待完成。386项focused通过；历史完整non-E2E结果仍保留。

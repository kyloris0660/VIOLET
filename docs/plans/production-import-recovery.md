# 生产导入可靠性修复与实际恢复

项目所有者于 2026-09-06 通过 28 号任务授权以下三个连续实施步骤，包含测试、必要增量迁移、受控生产部署与实际恢复。负责人复审和 PR 合并分别保留；执行代理不合并、不推 main、不追加 reviewer。

基线为已接受并合并的 PR #151：main `ea4bdd740943b2dad8c4eace88d0b33819d86cb8`，接受 HEAD `c08c84d77535d7b54494729526210c1bc11e694d`，接受与合并 tree `f1ed086101b46b1d657e8939985fa2eda7246dfe`。A1 候选、报告和契约结果只作为历史证据保留。

## 1. 失败隔离、记账与诊断

移除根据单项失败连续次数、累计数量或失败率中断手动同步的行为；保留总时限、取消、执行 cap，以及有具体证据的数据库/目标存储共同故障停止。读取与解码进程仍逐项限时，原异常类型、系统码、退出状态、耗时和文件版本进入私有 run-item。枚举在格式过滤前记录成员及 stat/目录异常，限定首次枚举资源。

复用来源和运行账本的 JSON 字段保存诊断、尝试时间、处置和续接信息，优先不增加表或列。去重按已存内容身份关联 Media，继承可证实的既有下游状态，保护人工整理和 A1 提交后恢复。

## 2. 公平候选与历史缺口

新候选、从未执行项和下游补做优先获得机会，到期旧重试保留有限份额；按真实最近尝试轮转，跨 cap 和中断保留队尾。重复失败可暂缓且可手动恢复，不以历史不可靠 attempt_count 判死刑；文件版本变化重新评估，主动忽略只由所有者选择。重试成功在本轮继续导入或下游。

复用已有诊断和补查附件，仅修有证据的未登记、错误 no-op、水位漏项和去重关联缺口。逐项保留历史与现场结果，新增观察单独记账。补查未回传的结论不得虚构。

## 3. 隔离验证、受控部署与真实恢复

在隔离数据库/本机测试文件验证超过旧阈值的首中尾失败、纯重试和混合队列、跨 cap/时限/重启、处置恢复、共同故障、stat 身份、去重及 A1。使用指定 venv、必要 PostgreSQL、focused、既有相关契约和文档检查；不重跑完整 non-E2E，不补造历史 AI 证据。

账本 JSON 更新在事务中按稳定 ID 和现场状态比较后写入，先保存受影响行及恢复方法，在副本验证，再用于原库。若必须新增 schema，先补具体可恢复迁移及副本证据；不覆盖恢复原库、不删除现有数据。生产仅通过已有 profile/controller 和日常入口切换，等待正在执行的用户任务完成。先部署候选时如实标注未合并，收到负责人合并结果后完成主线与启动锚点对齐。

本轮允许必要源读取、水合和导入复制；不修改源文件或云保留状态，不全库预下载。分类、WD、已有标签本地化仅处理本轮新增和待补做，复用配置、缓存与预算；Pixiv 读取保持开启、apply 关闭，不调用 Pixiv/第二 provider、不下载新模型。

最小 UI 修正区分新增、去重、实际失败、未执行、待补做和暂缓等处置，提供原因及恢复入口。正常 launcher、新会话、有界面系统 Edge 验证导入、普通搜索、图片浏览、原 5 样本/51 绑定。私有逐项证据及操作恢复记录仅保留本机；公开报告不包含源路径、凭据和原图。

# 当前交接

<!-- GENERATED: docs/state/current-phase.json -->

当前状态以 docs/state/current-phase.json 为准。

- 阶段：`PRODUCTION-PIXIV-A2`；状态：`PRODUCTION_PIXIV_A2_METADATA_ACQUISITION_AND_CONCEPT_IMPLEMENTATION`。
- 分支：`codex/production-pixiv-a2`；PR：`None`。
- 已接受并合并基线：PR #152 / `2b742ca3e49d4b7d361300e98e0b2d9c1a0eb63d`。
- 工程目标完成：`False`；负责人接受：`pending_project_lead_review`。
- 新LLM调用累计上限USD 10；原图不下载、不上传。

## 已完成检查点

- 项目所有者明确要求全量执行23号任务；39号负责人接受、PR151/152实际合并及日常入口对齐已核实。
- 可信fetch后main与origin/main均为2b742ca，无本地独有提交或工作树漂移；从该基线建立隔离A2分支，原工作区保持。
- 现场生产运行main 2b742ca，认证后server-identity与持久profile一致；Python身份预检通过。
- T0数据库只读快照完成：38114 Media，9460无歧义先验Media/9167 works/9370 pages，1497多先验Media；先验尚不等同于已确认绑定。A1仍5 Media/51绑定/1 active。
- 可信来源优先的固定映射：9502 Media、9209 works；1455 Media先验冲突保留，27157不适用。467可信绑定中的12个作者context兼容问题已在生产适配层处理。
- 原生产备份完成，独立任务数据库恢复通过；原库未覆盖恢复。副本及原库均完成队列准备，66页复用完整事实，6份预检输出本地重放未新增网络调用。
- 项目所有者确认令牌未轮换并明确授权本轮A2全部批次、重试与续跑使用；任务私有凭据许可已记录，未设置或宣称rotation confirmed，正常认证预检通过。
- 获取回归111项通过；固定Media/work/page绑定及累计事务替换已加入实现。原库有限清单获取运行中，进程级和HTTP请求级间隔至少2秒，CLI内部重试关闭，三次上限由持久日志统一控制。
- 已核查6429条历史持久裁决，角色/context/模型/版本相容性决定复用；初始42个候选完成缓存闭合，实际46次调用与失败均已计费保留，随后缓存重放未新增调用。
- 实际gpt-4.1-mini名称提取运行中，复用F7a验证及逐单元缓存；首批20单元完成并成功续跑。所有提取与配对裁决共用USD10账本，不重置预算。
- 共享概念的跨消费者upsert归属问题已修复：生产支持使用跨批次固定键空间，事务替换保留独立消费者。搜索/详情与所有权回归12项、局部失败及认证/网络暂停回归10项通过。
- 真实累计预览1733个有效work/page聚合、19086信号、102999候选边，构建22.796秒、峰值内存约1.20GB；角色提取尚未全部纳入，不能据此宣称最终全量质量或性能。
- 首组953名称单元全部完成。恢复副本531聚合的564配对已闭合：首轮319新调用、245缓存，4次传输失败保守计费；有界续跑561缓存、3新调用，最终无错误。随后累计2730名称单元继续缓存优先处理。
- 恢复副本已通过正常事务承接A1五样本，实际物化534 Media/3671支持，单active run；原生产仍为A1投影，尚未宣称全量生产完成。
- 固定范围、共享支持、分批顺序/续跑业务一致性等独立PostgreSQL9项通过；最新集中开发回归200项通过。嵌套系列ID误报及大小写代表值变化均已补回归，不新增重复获取或重复付费。
- A2可执行契约已注册，6项失败关闭回归通过。独立多语言答案及既有240-query已保留，最终真实质量、性能、浏览器及完整non-E2E仍待执行。

## 后续执行

1. 固定T0、兼容输入和累计所有权。
2. metadata获取、缓存复用和完整概念裁决。
3. 全量原生产落地、搜索/详情、质量与恢复验收。

## 范围和复用

- T0固定Media/work/page对应关系，T0后新增单独记账。
- 单先验不自动等同可信绑定；冲突有明确归宿。
- 真实metadata、保存payload和兼容judgments优先复用。
- 作者稳定ID保留，角色/作品所需context保留。

## 验证

- focused与隔离PostgreSQL覆盖实际变化。
- 一次完整non-E2E；历史缺失AI证据单列。
- 备份恢复、原子replacement、owned rollback及分批等价。
- 真实搜索/详情和正常launcher，记录当前全量性能。
- 工程完成由A2可执行结果契约检查。

## 操作边界

- 原生产在长时间获取和计算期间继续可用。
- 不重复导入、分类、WD标签和本地化基础工作。
- 不修改人工标签、相册、确认Entity或原文件。
- 不合并、不推main、不触发额外reviewer、不进入A3。
- 自动验证、负责人接受及产品用户体验分别记账。

下一检查点：持续获取固定清单，完成完整裁决与累计USD10控制、PostgreSQL恢复/搜索验证，再进行全量生产物化与真实浏览器验收。

## 持久入口

- [A2实施方案](../docs/plans/production-pixiv-a2.md)
- [执行runbook](../docs/development/agent-runbook.md)
- [生产启动器](../docs/production-launcher.md)
- [阶段契约](../docs/phase-contracts.md)
- [当前主线路线](../docs/roadmap/current-mainline-roadmap.md)
